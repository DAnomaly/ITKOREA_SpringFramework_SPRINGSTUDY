<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.koreait.file.dao.BoardDAO">

	<select id="selectListBoard" resultType="com.koreait.file.dto.Board">
		SELECT NO, WRITER, TITLE, CONTENT, FILENAME, POSTDATE FROM BOARD ORDER BY NO DESC
	</select>
	
	<select id="selectOneBoard" resultType="com.koreait.file.dto.Board" parameterType="long">
		SELECT NO, WRITER, TITLE, CONTENT, FILENAME, POSTDATE FROM BOARD WHERE NO = #{no}
	</select>
	
	<insert id="insertBoard">
		INSERT INTO BOARD VALUES (BOARD_SEQ.NEXTVAL, #{param1}, #{param2}, #{param3}, #{param4}, SYSDATE)
	</insert>
	
	<update id="updateBoard" parameterType="com.koreait.file.dto.Board">
		UPDATE BOARD
		   SET TITLE = #{title}
		     , CONTENT = #{content}
		     , FILENAME = #{filename}
		 WHERE NO = #{no}
	</update>
	
	<delete id="deleteBoard" parameterType="long">
		DELETE 
		  FROM BOARD
		 WHERE NO = #{no}
	</delete>

</mapper>